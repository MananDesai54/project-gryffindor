export enum Routes {
  HOME = "/",

  LOGIN = "/login",

  AGENT = "/agent",
  AGENT_CREATE = "/agent/create",
  AGENT_LIST = "/agent/list",
  AGENT_DETAIL = "/agent/:id",
  AGENT_WORKFLOW = "/agent/workflow/:id",
}
