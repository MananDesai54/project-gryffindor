{
  "name": "project-gryffindor",
  "module": "index.ts",
  "type": "module",
  "workspaces": [
    "packages/**/*"
  ],
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "scripts": {
    "dev:client": "bun run --cwd packages/client dev",
    "dev:server": "docker-compose up --build"
  }
}
